// Thin RPC helpers. The full covenant transaction-construction SDK is not yet
// published for Toccata (see docs/RISKS.md), so for now this package only owns
// connectivity + node-capability checks. tx-builder will depend on it once the
// SDK exposes covenant-bound output construction.
export {};
